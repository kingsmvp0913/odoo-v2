# 同構掛載路徑：宿主路徑必須與容器內路徑「完全相同」。
# 原因：平台在容器內 spawn `docker run -v <path>:...` 建立測試區時，該路徑是由「宿主的 docker
# daemon」解讀的；容器內外路徑不同會讓測試區掛到不存在的目錄，自訂 addons 全數遺失。
# 依你的實際位置調整後另存為 .env（.env 不進版控）。
HOST_REPO_DIR=/home/odoo/odoo-v2
HOST_ENV_BASE=/home/odoo/odoo-envs

# 時區。容器預設 UTC，不設會讓測試區夜間關機（預設 23:00）落在台灣早上 7 點。
TZ=Asia/Taipei

# —— 以下皆為「掛在既有網域下」才需要，留空＝維持本機直連行為 ——

# 測試區容器發佈到宿主的位址。預設每專案一個 127.0.0.x（cookie 依 host 隔離，多開不互踢 session）。
# 但反向代理的 nginx 若跑在另一個容器（bridge 網路），連不到宿主 loopback——此時設成 docker 橋接
# 閘道位址（`ip -4 addr show docker0`，多為 172.17.0.1）才反代得到。
# 注意：此位址會實際監聽，而測試區內有 seed 進去的固定帳密，設定前先確認該介面不對外網開放。
#ENV_BIND_HOST=172.17.0.1

# 使用者瀏覽器要開的測試區網址樣板。不設＝直接給 http://<綁定位址>:<port>，只有宿主本機連得到；
# 掛在公司網域下時該網址在使用者電腦上指向他自己，連結會點不開（本機重現不了）。
# 可用 {folder}（專案目錄名）／{port}／{host}。建議走 wildcard 子網域——每專案一個網域，
# cookie 隔離等價保留；Odoo 不支援掛在子路徑（/web、asset 路徑皆為 root-absolute），別用子路徑。
#ENV_PUBLIC_URL_TEMPLATE=https://{folder}.aidev.example.com
