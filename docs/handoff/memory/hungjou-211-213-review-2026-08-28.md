---
name: hungjou-211-213-review-2026-08-28
description: 鴻久 211/212/213 三張 review_pending 任務複審：213 有阻斷缺陷（active_test 漏帶、88 筆只改到 12 筆）、211 業務落差待使用者拍板、212 技術面過但漏排除下載暫存檔
metadata: 
  node_type: memory
  type: project
  originSessionId: eadcc8fb-e57b-4f13-9964-4aafa8b098d1
---

**處置（2026-08-28 已執行）**：使用者裁決後，經 `POST /api/tasks/:id/{approve,reject}`（自簽 userId=2 的 JWT，`JWT_SECRET` 在 `data/config.json`）→ **211 放行**（已進 wiki_updating）、**212／213 退回**（reject_triage，退回原因含完整 file:line 修正指示）。211 的規格 summary 措辭錯誤**沒有去改**——任務已放行、`analysis_yaml` 是歷史記錄，且平台 DB 只有唯讀通道。

2026-08-28 複審鴻久（project_id=3）三張停在 `review_pending` 的任務。三張的 QA 都 pass、都已部署到測試區（`/mnt/extra-addons/main` **同時疊著三張的碼**）。**三張都走 cs→analysis→coding→qa，沒有 E2E**（`e2e_disabled=true`）。

## 213「修改舊單據開帳」→ 應退回（阻斷）

`_collect_ini_status_code_drift`（`res_config_setting.py:1930`）用預設 `search()`，Odoo 自動加 `active=True`，而**開帳匯入的單一律 `active=False`**（`wizards/idx_init_import.py:1010` 寫死；同檔 :995 自己查詢時就有帶 `active_test=False`）。正式區實測：符合條件的鴻久 67 筆中 **56 筆封存改不到**、鴻伍 21 筆中 20 筆改不到（合計 88 → 只改得到 12）。畫面只報「已修正 11 筆」，**不會揭露漏改**。同檔既有慣例 `:1817`／`:2318` 都有帶 `with_context(active_test=False)`，這次沒跟。

- **QA 為何放行**：規格驗收樣本 E11-20260414003（id 77250）`active=t`，剛好是能改到的 11 筆之一。
- 那 56 筆是死路：批次被 active 濾掉、精靈按鈕因 `stage_closed=t and comp_status<>'0'` 全隱藏（`views/idx_maintenance.xml:148`）、表單 status_code 唯讀。
- 次要：`('comp_status','!=','0')` 會一併命中 NULL（Odoo `!=` 的固有行為），而匯入確實會寫 NULL（`idx_init_import.py:1033` `or None`）；正式區目前 0 筆、屬潛在。精靈那行 `if status_code=="1"` 也沒跟 `comp_status=="0"` 的條件同步。
- **使用者的前提「改成 4」是對的**：`idx_repair.py:3059-3064` 完工時 `status_code` 無條件寫 '4'、`comp_status` 原樣帶入，兩者正交；正式區 21,676 筆正常單無論 comp_status 為 1/2/3/4，status_code 一律 4。

## 211「安庫請購要排除萬用料號」→ 有條件通過（等使用者拍板）

2 行改動本身正確且實測生效。但**規格 summary 寫「改回維持原本手動轉請購」與實際行為不符**：萬用料號被排除後缺口不會消失，而是整筆落到 `_prepare_purchase_lines_after_material_issue` → 自動開專料 ODOO 請購並推 SM。同組資料對照實測（缺口 10）：安庫綁走 6 → 請購 **4**；綁走 10 → **0**；改動後 → **10**。採購量會實質變大。要問使用者接受哪一種。

- 另一支建立點沒改：`res_config_setting.py:1348` `_rebind_safety_gap`（設定頁「安庫請購綁錯舊單修正」按鈕）會 unlink 再用同料號重綁，**沒有 is_universal 判斷**。但實測既有 6 筆 not_arrived 的萬用列 `diff_days` 分別是 22／25／28 天，判 drift 的門檻是 **>30 天**且 `create_date` 與 `sm_source_key` 都不再變 → **既有列永遠不會被判 drift，零曝險**。
  - **使用者裁決 2026-08-28：那支工具不會再用到，不開修正單、只留觀察。** 監控錨點（下次碰鴻久安庫相關議題時比對即可，沒有告警機制）：鴻久 `idx_repair_purchase` id 4849／4850／4851／4871（皆 `O300-0001`、`sm_source_key=311-20260721022+O300-0001`、not_arrived）、鴻伍 11 筆。**這些 id 若消失＝那支工具終究被按了**；**若出現 `create_date` 晚於 2026-08-28 的萬用料號 `safety_stock` 列＝211 的排除沒生效**。
- 既有萬用料號安庫列：鴻久 4 筆（全 not_arrived、且**四張維修單綁同一張 SM 單 311-20260721022**，正是使用者說的誤配）、鴻伍 11 筆。改動不清舊；到貨同步 `action_sync_sm_received_qty` 路徑獨立，不受影響。

## 第二輪（同日）：213 放行、212 再退一次

- **213 第二輪通過**：三項全修好並端到端實測（8 fixture／冪等／匯出實產 xlsx／精靈四邊界）。附帶查清兩件事：`_ensure_maintenance_id` 不會讓批次整批 rollback（刻意造漂移仍寫入成功、正式區漂移 0 筆）；88 筆的 `tracking=True` 副作用可忽略（0.098 秒、**follower 為 0 所以無人收到通知**）。殘留邊界：`comp_status` 為 NULL 且未結案時精靈會靜默丟棄使用者選的完成狀態、該單之後按鈕與批次都碰不到，正式區可觸及 0 筆。
- ⚠ **「orphan 附件會讓一般使用者下載 404」是誤報，別再被騙一次**。複審 agent 用真實 HTTP 測到 404，看起來連已上線的 task 190 都中招。**但 Odoo 17 的 `sudo()` 不改變 current user**（core `models.py:5919-5920` 明寫），網頁情境下 `create_uid` 就是按下載的員工本人，`ir_attachment.check()` 的 `create_uid != env.uid` 不成立 → 讀得到。agent 是在 `odoo shell`（uid=1）造資料才讓 create_uid 變 OdooBot。**正式區反證**：190 產生的 26 筆孤兒 create_uid 全是真實員工、上線兩天無人回報。判讀法：看到「權限相關」的實測結論，先問它造資料的 uid 是不是真實情境。
- **212 第二輪再退**（第三輪修正中）。原因二：(1) `services/label_print_service.py:190` 的 `.csv` 仍寫死 `res_model`，而「列印標籤」必經該路徑，清單照樣每次多一列（這次只修了 `action_print_label` 尾端的 `.lab`）；(2) **既有暫存檔沒處理**——正式區鴻久 **615 筆／372 張單**、鴻伍 54 筆／28 張單，orphan 修法只防未來救不到它們，不處理的話使用者打開單子照樣看到一整排重複列。
  - **使用者裁決：不要刪檔，只要清單不顯示。** 故指示用 migration 把既有列的 `res_model`/`res_id` 清成 NULL（與新孤兒一致：實查為兩者皆 NULL），禁止 unlink／刪 filestore。
  - manifest 與**兩個正式區的已安裝版本都是 `17.0.1.0.5`** → migration 必須放 `17.0.1.0.6` 並升 manifest，否則同版本直接跳過（189 踩過的坑）。

## 第三、四輪：212 收尾（三張全數結案）

- 第三輪補了 `.csv`（`services/label_print_service.py:190`）＋ manifest `.5`→`.6` ＋ `migrations/17.0.1.0.6/post-migration.py`（UPDATE res_model/res_id 為 NULL，不 unlink）。**但 migration 用 `name LIKE '%.csv'`／`'%.lab'`**，會誤傷使用者手動拖進聊天室的同副檔名檔案 → 第四輪退回要求 JOIN `idx_maintenance` 改成 `a.name = m.maintenance_id || '.csv'` 精確比對，並在 UPDATE 前 SELECT 出 `(id, res_id)` 進 log。**理由是這支 SQL 不可逆**——res_id 清成 NULL 後就不知道原本掛哪張單。
- **收尾驗證手法（可複用）**：把 migration 的 SELECT 條件原封不動拿到正式區跑唯讀查詢，比對命中集合。收緊前後皆為鴻久 615 筆／372 單、鴻伍 54 筆／28 單 → 證明精確化沒漏抓也沒多抓。另拆分驗證 615 筆**全是**帶品號客戶名的下載暫存版、正規檔名 `{maintenance_id} 維修前報告.xlsx` 為 0 筆，確認不誤傷真報告。
- ⚠ **測試區已跑過第三輪的寬鬆版 migration（版本已是 `.6`）**，之後若再改這支 migration 內容就必須升 `.7`，否則測試區同版本不再執行（189 的坑）。正式區兩庫仍是 `.5`，升級時會執行最新版，故無影響。
- `.csv` 拿掉 res_model 不影響使用：下載 URL 只用附件 id、NAS 複製走 `file_content`、稽核靠 `_log_print_action` 的 `message_post`；且正式區該功能**從未被使用過**（`.csv` 附件與稽核訊息皆 0 筆）。
- 附帶效果：解除關聯後那些暫存檔**也會從右側 chatter 附件盒消失**（chatter 同樣查 res_model+res_id），正中使用者最初「右側訊息被洗版」的抱怨；照片的關聯未動，仍留在 chatter。

## 212「叫修單上傳圖片…」→ 技術面通過，另有既有漏洞

- ⚠ **不要把它判成「使用者只是提問卻被動手改」**——我派的複審 agent 這樣判了，但它看不到 cs 對話。實際上使用者在 cs 第 3 輪自己說「那如果單純改叫修單的報告總覽附件區塊把叫修單內上傳的照片排除掉這樣可以嗎?」、第 4 輪「可以那幫我送修改」，三個問句 cs 都答了（答案：會、是原生、不建議拿掉）。**審 review_pending 前先撈 `prompt_logs` 裡 cs-retry 的「【使用者這一輪說的】」，否則必然誤判需求被扭曲。**
- 改動經實測**不是 no-op**：照片走原生 `/web/binary/upload_attachment`，attachment 是 `res_field=False` + `res_id=叫修單id`，確實落在原 domain 內。`('id','not in',[])` 也不會清空清單。
- **漏排除（既有缺陷、非本次引入）**：叫修單層 `download_pre_repair_report`（`idx_maintenance.py:5820`）與 `action_print_label`（:6184）建的暫存檔仍帶 `res_model/res_id`，而既有過濾只比對 `pre_repair_report_filename`，兩邊檔名不同（下載版多了品號與客戶名）→ 按一次下載就多一列。task 190 修的是**維修單層**（`idx_repair.py`），叫修單層沒修。
- 副作用：照片從「報告總覽／附件」消失後，該清單每列的「下載」按鈕入口也一起沒了，要批次抓圖得回照片頁籤逐張點。

相關：[[hungjou-183-187-review-2026-08-25]]、[[odoo-test-env-shell-testing]]、[[qa-resume-stale-spec]]
