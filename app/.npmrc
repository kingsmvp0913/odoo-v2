# onnxruntime-node（@huggingface/transformers 的底層）預設會在 postinstall 時另外從 Nuget
# 下載 CUDA execution provider 的二進位——實測 302 MB，而平台是純 CPU 推論、沒有 GPU，
# 那 302 MB 從頭到尾用不到。CPU EP 本身包在 npm tarball 內，跳過 CUDA 不影響功能。
#
# 設在這裡而不是靠指令參數：start.sh／start.ps1 偵測到相依有異動會自行跑 npm install，
# 那條路徑帶不到參數，只有 .npmrc 管得到。
onnxruntime-node-install-cuda=skip
